const API='https://vortexcollections-api.onrender.com';
const token=localStorage.getItem('vortex_token');
const request=async(path,options={})=>{const r=await fetch(API+path,{...options,headers:{'Content-Type':'application/json',Authorization:'Bearer '+token,...options.headers}});const d=await r.json().catch(()=>({}));if(!r.ok)throw Error(d.error||'Não foi possível concluir.');return d};
const $=s=>document.querySelector(s);const msg=t=>{$('.portal-message').textContent=t};
async function loadPlans(){try{const plans=await fetch(API+'/api/plans').then(r=>r.json());const box=$('#plans');plans.forEach(p=>{const c=document.createElement('article');c.className='portal-card plan-card';c.innerHTML='<h2></h2><div class="plan-price"></div><p></p><button class="btn btn-primary">Assinar mensalmente</button>';c.querySelector('h2').textContent=p.name;c.querySelector('.plan-price').textContent=p.price_cents?'R$ '+(p.price_cents/100).toLocaleString('pt-BR',{minimumFractionDigits:2}):'Configure o valor';c.querySelector('p').textContent=Array.isArray(p.features)?p.features.join(' • '):String(p.features||'');c.querySelector('button').onclick=async()=>{if(!token)return location.href='index.html#cliente';try{const r=await request('/api/plans/'+p.slug+'/subscribe',{method:'POST',body:'{}'});location.href=r.checkout_url}catch(e){msg(e.message)}};box.append(c)})}catch(e){msg(e.message)}}loadPlans();
